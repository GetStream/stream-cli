{
	"git": {
		"tagName": "v${version}"
	},
	"github": {
		"release": true
	},
	"hooks": {
		"before:init": "yarn lint",
		"after:bump": "yarn manifest && yarn readme && yarn build",
		"after:release": "./github.sh"
	}
}
